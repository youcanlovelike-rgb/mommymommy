name: Build Mommy Real Core (Full Features)
on: [push, workflow_dispatch]

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: 📂 拉取源码
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: 🚀 增强内存与 Swap
        run: |
          sudo swapoff -a
          sudo fallocate -l 4G /swapfile
          sudo chmod 600 /swapfile
          sudo mkswap /swapfile
          sudo swapon /swapfile

      - name: 💉 包名全局替换 (Mommy 专属)
        run: |
          find . -type f \( -name "*.cpp" -o -name "*.h" -o -name "*.txt" -o -name "*.java" -o -name "*.list" -o -name "*.tl" \) -exec sed -i 's/org.drinkless.tdlib/org.mommy.messenger/g' {} +
          find . -type f \( -name "*.cpp" -o -name "*.h" -o -name "*.txt" -o -name "*.java" -o -name "*.list" -o -name "*.tl" \) -exec sed -i 's/org_drinkless_tdlib/org_mommy_messenger/g' {} +

      - name: 🛠️ 准备构建环境
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake gperf php-cli ninja-build clang

      - name: 🔐 锁定 OpenSSL (arm64-v8a)
        run: |
          git clone https://github.com/KDAB/android_openssl.git ../android_openssl
          CRYPTO_PATH=$(find ${GITHUB_WORKSPACE}/../android_openssl -name "libcrypto.a" | grep "arm64-v8a" | head -n 1)
          SSL_PATH=$(find ${GITHUB_WORKSPACE}/../android_openssl -name "libssl.a" | grep "arm64-v8a" | head -n 1)
          echo "CRYPTO_LIB=$CRYPTO_PATH" >> $GITHUB_ENV
          echo "SSL_LIB=$SSL_PATH" >> $GITHUB_ENV
          echo "SSL_INC=$(dirname $(dirname $CRYPTO_PATH))/include" >> $GITHUB_ENV

      - name: 🏗️ 第一阶段：生成真 API 代码 (灵魂注入)
        run: |
          mkdir -p build_local && cd build_local
          cmake -G Ninja -DTD_ENABLE_JNI=ON ..
          # 这一步必须跑通，它是产生真代码的唯一源头
          ninja td_api_generate_cpp td_generate_mime_types || echo "Gen partial"
          cd ..

          # 核心修复：清理旧目录并使用强力搬运
          rm -rf tdutils/generate/auto td/generate/auto
          mkdir -p tdutils/generate/auto td/generate/auto/td/telegram td/generate/auto/td/mtproto
          
          # 物理搬运：抓取所有真代码
          find build_local/td/generate/auto/ -name "*.cpp" -o -name "*.h" | xargs -I {} cp -v {} td/generate/auto/td/telegram/ || true
          find build_local/tdutils/generate/auto/ -name "*.cpp" -o -name "*.h" | xargs -I {} cp -v {} tdutils/generate/auto/ || true
          
          # 补齐 MTProto (解决 129 报错)
          find build_local/ -name "mtproto_api.*" | xargs -I {} cp -v {} td/generate/auto/td/mtproto/ || true

      - name: 🏗️ 第二阶段：全功能 Android 编译
        env:
          INC: ${{ env.SSL_INC }}
          CRYPTO: ${{ env.CRYPTO_LIB }}
          SSL: ${{ env.SSL_LIB }}
        run: |
          mkdir -p build_android && cd build_android
          # 强制指定生成的代码路径为当前物理路径，防止 clang++ 乱找
          cmake -G Ninja \
            -DCMAKE_TOOLCHAIN_FILE=${ANDROID_NDK_LATEST_HOME}/build/cmake/android.toolchain.cmake \
            -DANDROID_ABI=arm64-v8a \
            -DANDROID_PLATFORM=android-24 \
            -DTD_ENABLE_JNI=ON \
            -DOPENSSL_USE_STATIC_LIBS=TRUE \
            -DOPENSSL_INCLUDE_DIR="$INC" \
            -DOPENSSL_CRYPTO_LIBRARY="$CRYPTO" \
            -DOPENSSL_SSL_LIBRARY="$SSL" \
            -DCMAKE_INSTALL_PREFIX:PATH=../td_install \
            -DCMAKE_CXX_FLAGS="-I${GITHUB_WORKSPACE}/tdutils/generate/auto -I${GITHUB_WORKSPACE}/td/generate/auto" \
            ..
          
          # 锁定时间戳：告诉 Ninja 所有手动搬运的文件都是“最新的”
          find ${GITHUB_WORKSPACE} -type f -exec touch {} +
          
          # 启动！
          ninja -j2 install

      - name: 📦 上传全功能 libtdjni.so
        uses: actions/upload-artifact@v4
        with:
          name: mommy-full-real-core
          path: td_install/lib/libtdjni.so

